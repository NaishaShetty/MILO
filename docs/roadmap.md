# Roadmap

Two tables: a phase-completion history (audited against the Phase 8.7
findings — see below for what changed), and the genuinely future work
that remains. For the detailed per-phase implementation record, see
[`development_history.md`](development_history.md) and
[`phases/phase8_7_final_audit.md`](phases/phase8_7_final_audit.md).

## Phase completion history

| Phase | Description | Status |
|-------|-------------|--------|
| 1 | Simulator & Navigation | ✅ Done |
| 2 | Perception (detection, segmentation, scene graph, model management) | ✅ Done |
| 3.1–3.7 | Language Interface (spec, schema, parser assets, runtime, error recovery, benchmarking, API/frontend) | ✅ Done |
| 3.x | Spatial & Temporal Perception (depth, 2D→3D localization, tracking, temporal scene) | ✅ Done |
| 4 | Task Planning (rule-based, ReAct, and Behavior Tree planners, validator, symbolic `WorldState`, evaluation) | ✅ Done |
| 5 | Execution & Action Control (dispatcher, precondition/result validation, error taxonomy) | ✅ Done |
| 6.1–6.2 | Memory Foundation & Intelligence (episodic/semantic/failure memory, SQLite + vector retrieval) | ✅ Done |
| 6.3 | Memory ↔ Robot Integration (memory-conditioned planning, retrieve→plan→execute→remember loop) | ✅ Done |
| 6.4 | Memory research evaluation (memory-vs-no-memory benchmark) | ✅ Done — originally `FakeSimulator`-only; re-run against real AI2-THOR + a real embedder in Phase B (see [`experiments/reports/phase_b_real_ablation_findings.md`](../experiments/reports/phase_b_real_ablation_findings.md)) |
| 7 | Multi-agent `Orchestrator`, Reflection, dynamic replanning, Speech (Whisper) | ✅ Done — corrects the pre-8.7 README, which listed Reflection and Speech as "Planned" |
| 8.1–8.6 | MILO frontend (7 pages), ElevenLabs voice, MILO Lab, Docker/CI, configuration hardening | ✅ Done |
| 8.7 | Final research-readiness audit, critical bug fix (target/target_location), README restructure | ✅ Done |
| C | Vision-grounded planning: `SpatialScene → WorldState` translation layer (`backend/planner/grounding.py`), existence/location grounding only at the time | ✅ Done — `Orchestrator._observe()` now grounds `WorldState` from a live `Scene` before planning and before each replan, not just for Mission Control display. Extended to `is_open`/`robot_holding` in a later "E follow-up" row (Full vision state fusion) — see that row for what's grounded now and what's still open. |
| D | Floor-plan generalization sweep: 5 real AI2-THOR scenes (all 4 iTHOR room types), 3 tasks each, unmodified pipeline | ✅ Done — 13/15 succeeded; both failures have concrete causes (one real AI2-THOR geometry limit, one genuine `_deposit()` planner bug this sweep newly found). See [`experiments/reports/phase_d_floorplan_generalization_findings.md`](../experiments/reports/phase_d_floorplan_generalization_findings.md). |
| E | `milo_benchmark v1.0`: versioned 25-task/5-scene/3-tier dataset (`backend/planning_evaluation/dataset/v1.0/`), planner-comparison + memory-ablation runners, live post-execution goal-check scoring | ✅ Done (dataset + runner + real baselines) — original run: `rule_based` 23/25 (92%), `behavior_tree` 22/25 (88%); memory ablation re-confirmed across all 5 scenes. **These two numbers are historical** — both non-geometry failures were fixed by the "E follow-up" rows below, and a full re-run puts both planners at 24/25 (96%); see the "E follow-up" rows and Addendum 3 for current numbers. `ReActPlanner` baseline and Hugging Face Hub publish were tracked separately below at the time (no LLM key / no HF token in this environment then) — see the "E follow-up" ReAct rows for how that was resolved. See [`experiments/reports/phase_e_milo_benchmark_report.md`](../experiments/reports/phase_e_milo_benchmark_report.md). |
| E follow-up | `_deposit()` non-openable-target fix: added `ObjectState.is_openable` (`planner/state.py`), `_deposit()` now skips `open`/`close` when a `store`/`place` target is known-`False` openable, `_seed_initial_state_from_live_metadata` seeds it from AI2-THOR's own `openable` flag | ✅ Done — re-ran `milo-v1-fp401-t3a` (spray bottle → shelf) against real AI2-THOR: now succeeds (`goal_success=True`), was the one non-geometry `tier3_store` failure in the Phase E baseline. Regression tests: `test_store_goal_has_no_open_close_for_a_known_non_openable_target`, `test_store_goal_still_opens_a_target_with_unknown_openable_state` (`tests/test_planner_rule_based.py`). See the Phase D report's resolution addendum. |
| E follow-up | Misleading `ACTION_TIMEOUT` message root-caused and fixed: `ai2thor.fifo_server.FifoServer` has its own internal ~100s pipe-read timeout, unrelated to and invisible to this project's `step_timeout_s`; since Python 3.11 `concurrent.futures.TimeoutError is TimeoutError`, so AI2-THOR's own timeout was landing in `execution/controller.py`'s `except FutureTimeoutError:` clause and being misattributed to a `step_timeout_s` that was never configured (producing `"exceeded its Nones timeout"`) | ✅ Done (message fixed; the underlying ~100s Unity stall itself is an AI2-THOR/Unity characteristic, not a bug in this project, and is not "fixed" — see the addendum for why that distinction matters) — `_dispatch_with_retries` now reports which timeout actually fired, with AI2-THOR's own real number included. Regression test: `test_engine_level_timeout_with_no_step_timeout_configured_is_reported_accurately` (`tests/test_execution_controller.py`). See `experiments/reports/phase_e_milo_benchmark_report.md`'s resolution addendum. |
| B follow-up | Memory-hint `parentReceptacle` gap root-caused and fixed: real AI2-THOR 5.0.0 populates a separate, previously-unread `parentReceptacles` (plural, list) metadata field reliably for every loose object, even when the deprecated singular `parentReceptacle` this project's code exclusively read is always `None`; `TaskRunner._form_observation()` now prefers the plural field (falling back to singular for synthetic `FakeSimulator` scenarios, which only ever populate the singular one) | ✅ Done — re-ran the "find X twice" recall ablation across all 5 Phase D scenes: memory hint now engages on 5/5 `memory_on` recall episodes (was 0/5), confirmed via each episode's plan gaining the expected extra `locate`/`navigate` pair on the remembered location. See the Phase B report's resolution addendum. |
| E follow-up | Real `ReActPlanner`/Gemini baseline attempted on `milo_benchmark v1.0` (`LANGUAGE_LLM_PROVIDER=gemini`, `gemini-flash-latest`) | ⚠️ Attempted, not a clean baseline — Gemini's free tier hit its daily quota (20 requests/day for this model) partway through the run; only 2/25 episodes got a real plan produced+executed. See `experiments/reports/phase_e_milo_benchmark_report.md`'s Addendum 2. Superseded by a clean local baseline, next row. |
| E follow-up | Real `ReActPlanner` baseline via local Qwen/Ollama (`qwen2.5:7b`, Q4_K_M, served by Ollama on an RTX 4050 Laptop GPU, 6GB VRAM, 82%/18% GPU/CPU split) — no quota, no fallback planner | ✅ Done — clean 20/25 (80%) on the full `milo_benchmark v1.0` set: 10/10 `tier1_locate`, 10/10 `tier2_pickup`, 0/5 `tier3_store`. `goal_success`/`execution_success`/`plan_success` agree on every episode (no predicate-artifact this time). All 5 failures are genuine multi-step reasoning failures (precondition-order mistakes), not infrastructure — none needed the transient-error retry wrapper. `rule_based`/`behavior_tree` re-confirmed at 24/25 (96%) each in the same run. See `experiments/reports/phase_e_milo_benchmark_report.md`'s Addendum 3 for full setup/reproducibility detail and the per-task failure breakdown. |
| Test infra | `backend/tests/conftest.py` added — forces `VISION_ENABLE_SIMULATOR=false` for every test under `backend/tests/`, regardless of a local `backend/.env` setting it `true` for interactive dev (`api/app.py`'s `load_dotenv()` only fills gaps in `os.environ`, so the `.env` value otherwise leaked into every `TestClient`) | ✅ Done — fixed two things at once: (1) 3 tests asserting the "no simulator configured" `503` case were seeing a real simulator instead and failing; (2) nearly every `TestClient` construction across the suite was incidentally loading real Grounding DINO/SAM2 weights via the app's lifespan, which is why the full suite took 13–14 minutes — with the leak fixed it now runs in ~6 seconds. `test_api_app_lifecycle.py`'s own tests (which intentionally toggle this var via `monkeypatch`) are unaffected. **Independent of benchmark/dataset validity** — this is a pytest-only env-leak fix; the real `run_benchmark.py`/`run_floorplan_sweep.py`/etc. runs set their own env vars explicitly (`RUN_SIMULATOR_TESTS=true`) and were never affected by this. Documented in `docs/testing/running_tests.md`. |
| E follow-up | Publish `milo_benchmark v1.0` to the Hugging Face Hub | ✅ Done — live at [huggingface.co/datasets/naishashetty/milo_benchmark](https://huggingface.co/datasets/naishashetty/milo_benchmark) (`tasks.json` + dataset card, uploaded via `huggingface_hub.HfApi.upload_folder()`). |
| E follow-up | Perception-grounded `tier1_locate` check: wire Phase C's `ground_world_state()` into the benchmark harness post-execution as a second signal, `perceived_by_agent`, alongside the existing `exists_in_scene` check | ✅ Done (infra) — `live_state.check_goal_live_grounded()` + `run_benchmark.py` now run a real `GroundingDINODetector`/`SAM2Segmenter` stage against the live simulator's post-execution camera frame for every `tier1_locate` episode, real, on all three planners. `goal_success` is deliberately unchanged (still `exists_in_scene`); `perceived_by_agent` is reported side by side, never merged in. See `experiments/reports/phase_e_milo_benchmark_report.md`'s Addendum 5. **The infrastructure works; what it found is a separate, NOT-done item — see the next row.** |
| E follow-up | Vision sim-to-real detection gap (surfaced by the row above) | ✅ Fixed — root-caused: `GroundingDINODetector`'s production `box_threshold=0.35` misses real detections that peak around confidence 0.275 on AI2-THOR's synthetic renders (`perceived_by_agent` came out 6/10 `rule_based`, 6/10 `behavior_tree`, 5/10 `react` against ground-truth-visible objects). A lowered threshold (0.15) recovered most misses but was **explicitly not adopted** at the time -- false-positive rate was never measured. That measurement is now done: a real precision/recall validation set (`planning_evaluation/validate_detection_threshold.py`) across 8 real AI2-THOR scenes -- 9 true positives (ground-truth-`visible` objects, one real camera frame per scene, navigated toward the target) and 14 true negatives (objects confirmed absent from that scene's own live metadata, not synthetic) -- swept `box_threshold` at 0.15/0.20/0.25/0.30/0.35 (`text_threshold` held at 0.25, matching production's unchanged value). Result: **0.25 (and 0.30, identical in this sample) dominates both endpoints** -- same recall as 0.15 (77.8%, TP=7/9) with meaningfully better precision (63.6% vs 53.8%, FP=4 vs FP=6), AND better recall than 0.35 (77.8% vs 55.6%, TP=7 vs TP=5) with equal-or-better precision (63.6% vs 62.5%). Adopted: `GroundingDINODetector`'s default `box_threshold` changed 0.35 → 0.25 (`text_threshold` unchanged). Regression-tested (`tests/test_grounding_dino_detector_config.py`, inspects the constructor default directly -- no model weights loaded, matching CI's no-GPU scope). **Caveat on the comparison**: the sweep used a *symmetric* threshold (`box_threshold == text_threshold`) at each point; production's real 0.35 config paired that box threshold with the already-lower `text_threshold=0.25`, so the "0.35" row in this validation is a slightly stricter proxy for old production, not an exact match -- real prior recall was likely marginally higher than the 55.6% measured here. The core comparison (0.25 vs 0.15) is unaffected by this, since both used the same `text_threshold=0.25` production already had. See that script for the full per-scene raw data. |
| **New finding** | **Multi-phrase joint-prompt interference — a real, broader detection-reliability issue distinct from the threshold gap above and from the tier4 held-object gap below.** Surfaced while diagnosing `fp302-t4a` (`diagnose_fp302_zero_detection.py`): the same held alarm clock scored 0.1848 confidence under a 4-phrase joint prompt (`"alarmclock. cd. drawer. shelf."`) vs. 0.4844 under a single-phrase query, on the *identical* frame -- Grounding DINO's phrase-grounding degrades when multiple object names are queried jointly, likely from phrase/label merging. **Confirmed broader, not tier4-isolated**: re-tested with a second, independent object/frame (`mug` in `FloorPlan1`, under the frontend's actual default Vision-panel prompt, `"chair. table. mug. apple. bottle. refrigerator."` -- `frontend/src/components/VisionPromptForm.tsx`'s `DEFAULT_PROMPT`) -- confidence dropped from 0.2431 (single-phrase) to 0.1586 (6-phrase joint), the same direction and a comparable relative magnitude. **Call sites checked**: `orchestration/orchestrator.py`'s live `_observe()` and `planning_evaluation/live_state.py`'s `check_goal_live_grounded()` (the actual `tier1_locate` scoring path) both already use single-object prompts only -- **not affected**, live task planning and benchmark scoring are safe. `agents/vision_agent.py`'s `.locate()` always defaults to a single-label prompt and is never called with a multi-phrase override anywhere in this codebase -- **not affected**. Confirmed **affected**: (1) the frontend's own default Vision-panel prompt reaching the live `POST /api/v1/vision/perceive` endpoint (`api/routes/vision.py`) -- a real, user-facing path, not hypothetical; (2) `planning_evaluation/validate_detection_threshold.py`'s own validation methodology, which intentionally joins true-positive and true-negative names into one prompt per scene -- the *relative* threshold comparison (0.25 vs. 0.15 vs. 0.35) is likely still valid since the same joint-prompt methodology applied uniformly across every sweep point, but the *absolute* recall numbers reported above likely understate what per-name querying would achieve; (3) several manual demo/verification scripts (`vision/test_detector.py`, `scene/test_scene.py`, `vision/segmenters/test_segmentation.py`, `vision/visualization/test_visualizer.py`), all using 5-6-phrase joint prompts -- non-critical (not part of any automated/scored path) but would mislead anyone reading their console output as representative confidence.

**Checked and ruled out**: `vision/state/open_state_classifier.py`'s own `"open. closed."` two-phrase prompt was the most obvious suspect (structurally identical pattern to the confirmed cases above, shipped this same session), so it was directly re-tested rather than assumed -- diagnostic re-run of the same 3 live fridge frames (`vision/state/test_open_state_classifier.py`'s scenario) using per-phrase queries instead of the joint prompt. Result: per-phrase querying did **not** improve accuracy -- it made it worse (1/3 correct vs. the shipped joint prompt's 2/3), predicting "open" in all three frames regardless of actual state (open_score beat closed_score by a similar margin, ~0.23, every time -- a fixed bias, not phrase-suppression being relieved). **The documented 2/3 accuracy is real, not understated** -- this classifier's imperfect accuracy is a genuine, separate limitation (something about how a refrigerator crop's visual content maps to "open"/"closed" phrase-grounding, independent of query framing), not an instance of the joint-prompt-interference bug. No fix bundled here; a future accuracy pass on this classifier is a different problem from the multi-phrase-prompt fix.

**Not fixed in this pass, deliberately** -- this is a newly-confirmed, real, broader finding to decide on separately (e.g. whether to change the frontend's default prompt, whether `validate_detection_threshold.py` should be re-run with per-name queries for a cleaner absolute number), not something to fold into the tier4/depth-cutoff commit that surfaced it. |
| E follow-up | Re-tested the two known-failing `tier4_multi_step` episodes (row above) at the newly-validated `box_threshold=0.25`, to check whether the detection-confidence fix actually unblocks vision-grounded held-object detection as hypothesized | ⚠️ First-pass result, superseded by two further investigation passes -- see the `tier4_multi_step` WorldState-reseeding row (a few rows down) for the final, precise per-episode status. Summary of what this pass found: at 0.25 with the SAME joint multi-phrase detection prompt used before, `milo-v1.1-fp302-t4a` still produced zero detections on the critical frame and `milo-v1.1-fp201-t4a` produced real detections (`book` at 0.713m-1.583m) that didn't clear the held-object heuristic's 0.5m cutoff. Follow-up investigation found the `fp302` zero-detection result was itself an artifact of the joint multi-phrase prompt, not a genuine detection failure -- see the reseeding-gap row for the real root cause and fix. |
| E follow-up | CPU-bound vision inference (surfaced by the same run) | ✅ Fixed — root cause: `pip install torch`/`torchvision` on this machine (Python 3.14, no matching pre-2026 CUDA wheel published at the time) silently resolved to the CPU-only build; `torch.cuda.is_available()` was `False` despite the RTX 4050 being present and idle. Fixed by installing the CUDA 12.6 wheel set explicitly (`pip install torch==2.13.0+cu126 torchvision==0.28.0+cu126 --index-url https://download.pytorch.org/whl/cu126`, plus the `nvidia-*-cu12` runtime packages it depends on — installing with `--no-deps` breaks it, since those aren't bundled in the wheel itself). Verified past the flag: ran real `GroundingDINODetector`/`SAM2Segmenter` inference and confirmed via `nvidia-smi` sampled during the call that GPU utilization actually spiked (75-89%) and VRAM was actually allocated, not just `torch.cuda.is_available()` returning `True`. Same-machine, same-image, same-code-path before/after comparison (40-41 detections/frame, 5 iterations, first excluded as warmup): `GroundingDINODetector.process()` 6189ms (CPU) → 511ms (GPU), ~12.1x; `SAM2Segmenter.process()` 39120ms (CPU) → 4734ms (GPU), ~8.3x. Addendum 6's own per-planner wall-clock table is unaffected by this fix -- it explicitly excludes vision-inference time by design (captured before the perception-grounding check runs), so `rule_based`/`behavior_tree`/`react` numbers there don't change. The Addendum 5 sim-to-real detection-*confidence* finding (0.35 threshold missing real detections around 0.275) is a model-weights/threshold question, not a CPU-vs-GPU one -- expected to reproduce identically under GPU inference, but not re-verified here; only latency was re-measured. **VRAM-contention follow-up, now resolved (see next-but-one row)**: a prior pass flagged vision+Ollama alone at ~96% of this card's 6.1GB VRAM as a real risk for the vision-state-fusion work, since that work also needs AI2-THOR/Unity running concurrently, untested at the time. Real AI2-THOR + vision inference running together (Ollama not required for that specific work) was measured directly: peak ~2.4GB/6.1GB (~39%) -- comfortable headroom, not a blocker. See the vision-state-fusion row below. |
| E follow-up | `milo_benchmark v1.1`: scale-up to 9 scenes/54 tasks + new `tier4_multi_step` tier (two independent single-object `store` sub-goals per task, run as two sequential `TaskRunner.run()` calls against one live episode) | ✅ Done (dataset + runner + real baselines) — `rule_based`/`behavior_tree` 50/54 (92.6%) each (18/18 tier1, 18/18 tier2, 7/9 tier3, 7/9 tier4); `react`/`qwen2.5:7b` 36/54 (66.7%) (18/18, 18/18, 0/9, 0/9 — inherits `tier3_store`'s existing 0% rate, arithmetically expected for a tier requiring two consecutive successes). `v1.0` stays frozen/unchanged on every scoring-relevant field (regression-tested; the free-text `notes` on 2 tasks were deliberately edited, one substantively -- see the dataset card). `execution_success` matches `goal_success` on all 162/162 episodes; `plan_success` diverges from both on 8/162 (all `rule_based`/`behavior_tree`, real execution-time physics failures after a symbolically valid plan -- see Addendum 7). See `experiments/reports/phase_e_milo_benchmark_report.md`'s Addendum 7 and `backend/planning_evaluation/dataset/v1.1/README.md`. |
| E follow-up | `tier4_multi_step` surfaced a real WorldState-reseeding gap: a failed `place` step leaves an object physically held, but `_seed_initial_state_from_live_metadata()` has no "hand already occupied by a leftover object from a prior failed sub-goal" signal, so the next sub-goal's planner issues a `pickup`/`navigate` AI2-THOR then rejects (`"Agent hand has something in it already!"` / teleport/hand-collision error) | ⚠️ Partially fixed, precisely understood, not fully closed -- three real investigation passes, summarized here (full detail: `docs/architecture/spatial_perception.md`'s Planner-boundary section, `backend/planning_evaluation/investigate_tier4_vision_holding.py`, `validate_held_object_depth.py`, `diagnose_fp302_zero_detection.py`). **`milo-v1.1-fp302-t4a`: the reseeding gap's own specific symptom (an engine-level crash from the planner blindly issuing a doomed action) is fixed.** Root cause of its earlier zero-detection result: not a genuinely-invisible object, but a *multi-phrase prompt-interference* artifact -- the same held alarm clock scored 0.1848 confidence under a 4-phrase joint prompt (`"alarmclock. cd. drawer. shelf."`) vs. 0.4844 under a single-phrase query, on the *identical* frame (`diagnose_fp302_zero_detection.py`). Re-seeding with one detection call per object name (not one joint prompt) at the validated `box_threshold=0.25` correctly detected the held alarm clock at 0.398m (within the depth cutoff), seeded `robot_holding='alarmclock'` correctly, and the planner then correctly *declined* to plan sub-goal 2 instead of crashing AI2-THOR. `goal_success` for the full task is still `False`, but now solely because sub-goal 1 genuinely fails on a real, separate, pre-existing AI2-THOR placement-geometry limit (the same class as `FloorPlan301`/`FloorPlan203`'s known `tier3_store` failures) -- not because of this bug. **`milo-v1.1-fp201-t4a`: still open, and now understood precisely, not just "zero detections."** With per-name queries, the actually-held book WAS detected (0.713m) -- but that depth falls outside `HELD_OBJECT_MAX_DEPTH_M=0.5m`, so it was correctly left unflagged per that cutoff's own logic; a real, measured, separate calibration finding is why: a clean, confirmed-`isPickedUp` held-book measurement came out at 0.853m, a different object-size regime than the small objects (0.347m-0.459m) the cutoff was originally calibrated against (see the vision-state-fusion row below). Worse: a different, NOT-held nearby box (0.345m) was closer and got wrongly seeded as `robot_holding` instead -- a real, live instance of the heuristic's documented "closest wins regardless of label" limitation, not a hypothetical. Investigated whether raising the depth cutoff would help: real precision/recall data (below) shows it would trade away far more than it gains, so **not adopted**. Concrete next step, not done in this pass: seed `robot_holding` from AI2-THOR's own live `isPickedUp`/`inventoryObjects` metadata in `_seed_initial_state_from_live_metadata()` -- a purely symbolic fix that sidesteps both the detection-prompt and depth-calibration dependencies entirely, and the most promising path to fully closing this gap. Regression tests for the real, confirmed limitations: `tests/test_planner_grounding.py::test_large_held_object_beyond_default_cutoff_is_not_flagged_as_held`, `::test_closer_not_held_object_is_wrongly_preferred_over_farther_held_one`. |
| E follow-up | Second local model for `react`: `qwen2.5:3b` (Q4_K_M, same RTX 4050/Ollama setup) run against the identical `milo_benchmark v1.1` 54-task set `qwen2.5:7b` was scored on | ✅ Done — `qwen2.5:3b` matches `qwen2.5:7b`'s 36/54 (66.7%) goal-success rate exactly, task-for-task (0/54 differences, verified via a full side-by-side table, not just matching totals), at ~2.3x lower average latency (3.1s vs 7.2s/episode) and full GPU residency (vs. 7b's 82/18 GPU/CPU split). Investigated the identical aggregate score by re-running 6 failing episodes (3 per model) with a diagnostic wrapper that captures the actual raw LLM completions (never persisted by the normal run) — reproduced the same outcomes, and corrected an initial mischaracterization based only on each episode's final error string: both models' failures share a more precise root cause than first reported — **neither model's proposals ever include a `locate` call for the destination/container object**, only sometimes for the primary object. `qwen2.5:3b`'s proposals stall immediately at that gap; `qwen2.5:7b`, when it gets further, correctly completes locate/navigate/pickup on the primary object but then supplies the container's name to `put_down`/`place`'s `target` field, which the schema defines as the *held object's* identity — a wrong-value mistake, not a missing one. Verified directly on 6 episodes, not re-checked against all 27 originally-classified violations. See `experiments/reports/phase_e_milo_benchmark_report.md`'s Addendum 8. |
| E follow-up | `react`'s local-model `tier3_store`/`tier4_multi_step` failures (both `qwen2.5:3b` and `qwen2.5:7b`) share a real LLM reasoning limitation: neither model's proposals ever include a `locate` call for the destination/container object, only (sometimes) for the primary object being moved -- and `qwen2.5:7b` additionally confuses which object identity belongs in `put_down`/`place`'s `target` field, supplying the destination's name where the schema expects the currently-held object's name | ⚠️ Open, tracked observation -- **not a MILO code bug, not being patched**. `planner.actions.py`'s validator/precondition logic (`_require_target_located`, `_require_holding_target`, etc.) is working exactly as designed in every one of these episodes -- it is correctly rejecting real, structurally invalid proposals; weakening or working around that validation to let these proposals through would defeat the whole point of `ReActPlanner`'s "the LLM never bypasses the planner framework's own precondition checks" design (see `react.py`'s own docstring). This is a model capability/prompting limitation, out of scope for a validator fix. Verified directly on 6 real re-run episodes (3 per model) with full raw-completion capture, not re-checked against all 27 originally-classified precondition violations from the full run -- see `experiments/reports/phase_e_milo_benchmark_report.md`'s Addendum 8 for the actual transcripts. **Possible future direction, not committed to in this pass**: refining `react.py`'s system prompt (`_build_request`) to explicitly require a `locate` call on both the primary object and the destination before any `navigate`/`place` step might reduce this failure mode -- worth testing later, not implemented here. |
| E follow-up | Raw LLM completion logging gap in `planner.react` -- `_propose_with_repair()`'s `logger.warning("planner.react.malformed_output", extra={...})` passed the raw completion nowhere a default log formatter would render it (`extra=` dict values are invisible under the default `logging.Formatter`), which is exactly what forced the row above's investigation to re-run 6 episodes with a one-off diagnostic `LLMClient` wrapper just to see what a model had actually proposed | ✅ Done -- `_propose_with_repair()` now logs every raw completion (accepted or rejected) via `logger.debug("planner.react.raw_completion task_id=%s attempt=%s content=%r", ...)`: the content is interpolated directly into the message string (not `extra=`), so it renders under any formatter, and it is DEBUG-level only, so it produces zero output at this project's normal/default logging configuration -- a caller has to explicitly opt into `DEBUG` on the `planner.react` logger to see it (e.g. for exactly this kind of investigation on one benchmark run). The existing short `planner.react.malformed_output` WARNING (task_id/attempt/error, no raw content) is unchanged. Regression-tested for real: `test_raw_completion_is_captured_and_readable_at_debug_level` (raw content actually appears in rendered log output, not just that the log call doesn't crash) and `test_raw_completion_is_silent_at_default_warning_level` (confirms the gating -- zero output at WARNING) in `backend/tests/test_planner_react.py`. Future `react` failure investigations no longer need a special diagnostic re-run to see this. |
| E follow-up | Full vision state fusion: extend `ground_world_state()` for `is_open` (`backend/vision/state/open_state_classifier.py`, a Grounding-DINO phrase-grounded open/closed classifier reusing the already-loaded detector -- no new model) and a depth-proxied `robot_holding` heuristic | ✅ Done (infra) -- both signals are real, wired into `ground_world_state()`, and unit-tested (`backend/tests/test_planner_grounding.py`, including a vision-vs-symbolic disagreement case: vision overwrites a stale symbolic `is_open` belief; the held-object heuristic only ever adds positive evidence, never clears `robot_holding` to `None`). Real smoke test against a live AI2-THOR fridge (`vision/state/test_open_state_classifier.py`): 2/3 correct -- closed detected correctly both times, the open case missed (classifier said closed when the fridge was actually open). VRAM checked directly: real AI2-THOR + real vision inference running concurrently peaks at ~2.4GB/6.1GB (~39%), comfortable. **Marked "infra," not "solved"**: the held-object heuristic's real-world usefulness for the exact gap it was built to help with (`tier4_multi_step`'s WorldState-reseeding gap, see that row above) is now precisely understood after three full investigation passes -- it genuinely fixed that gap's crash symptom for one episode (`fp302-t4a`), but a real, measured calibration limit (held-object depth varies by object size -- 0.347m-0.459m for small objects, 0.853m for a book) leaves it unable to help the other (`fp201-t4a`), where it can even misattribute holding to a different, closer, NOT-held object instead. Depth alone is a real, useful, but not fully reliable held-vs-not-held signal once object size varies -- not a solved problem, and not being forced into looking like one. Appearance-model-grade accuracy for either signal has not been measured at benchmark scale; only real, small, honestly-reported smoke tests and calibration runs exist so far. See `docs/architecture/spatial_perception.md`'s Planner-boundary section. |

## Future work

Audited against the Phase 8.7 implementation — items already built (Reflection,
dynamic replanning, memory-conditioned planning for the rule-based path, Speech)
are removed from this list; only what's genuinely still open remains. Three
more items (memory threaded into the ReAct prompt, the rule-based
closed-receptacle gap, the two orchestration entry points) were closed out in
a follow-up pass — see `backend/planner/react.py`, `backend/planner/
rule_based.py`'s `_deposit()`, and `backend/orchestration/task_runner.py`'s
"Status: legacy single-attempt entry point" docstring section. A further
follow-up (Phase B) re-ran the Phase 6.4 memory ablation against real
AI2-THOR + a real embedder rather than `FakeSimulator`/a lexical embedder —
see [`experiments/reports/phase_b_real_ablation_findings.md`](../experiments/reports/phase_b_real_ablation_findings.md)
for the result (the 90%→80% finding didn't reproduce, and why) and the new
item below it identified as the concrete next step.

| Item | Why it's not done |
|---|---|
| HTN planner | Never implemented, despite being one of the originally scoped planning strategies. |
| True path-planning navigation | `Simulator.navigate_to()` teleports to an interactable pose (AI2-THOR's `GetInteractablePoses` + `TeleportFull`), not a walked path — a documented scope decision, not a bug. |
| Learned scene graph | Only a heuristic (`HeuristicSceneGraph`) exists; a model-based `BaseSceneGraph` implementation is future work. |
| Real-scene perception benchmark | `backend/vision_evaluation/` is synthetic/deterministic; a curated, labeled AI2-THOR dataset is future work. |
| Appearance-based re-identification | `IoUTracker` never re-matches a track after it's `LOST`. |
| Production hardening of the API | ❌ Not planned — MILO's actual deployment model (local GPU host + AI2-THOR/Unity, no publicly reachable frontend or backend) has no publicly reachable API to protect, so there's no real problem to solve here. Would only become relevant if that deployment model changes (e.g. a public-facing deployment is actually stood up). Documented decision, not an oversight. |
| Conversational clarification loop | The frontend surfaces `clarification_reason` but has no follow-up-question flow. |
| Broader scene/task diversity beyond the Phase D sample | Phase D validated 5 of ~120 iTHOR scenes (one per room type plus a second kitchen), 3 tasks each — real signal, not a statistically powered study. See [`experiments/reports/phase_d_floorplan_generalization_findings.md`](../experiments/reports/phase_d_floorplan_generalization_findings.md). |
